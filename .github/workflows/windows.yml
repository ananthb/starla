name: Windows

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@nightly

      - name: Build probe
        run: cargo build --bin starla

      - name: Run tests
        run: cargo test --workspace --exclude starla-tray

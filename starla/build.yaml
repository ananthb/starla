build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base:3.21
  amd64: ghcr.io/home-assistant/amd64-base:3.21
args:
  # Upstream starla release this add-on ships. Must name a tag that exists
  # at ghcr.io/ananthb/starla; the add-on version in config.yaml moves
  # independently. See the comment in Dockerfile.
  STARLA_VERSION: "0.7.1"
